# IBVAP Frontend — Production / Staging
# Override VITE_USE_MOCK to "false" once the FastAPI backend is deployed.
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_USE_MOCK=true
VITE_WS_URL=ws://localhost:8000/ws/detections
